node_modules/
.next/
out/
.env
.env.local
.env.production.local
*.tsbuildinfo
.vercel

# Credentials — defense in depth so an accidental copy never ends up staged
*-firebase-adminsdk-*.json
*serviceAccount*.json
*service-account*.json
*.pem
*.key
secrets/

# OS / editor cruft
.DS_Store
*/.DS_Store

# Source logo archives — the cleaned/renamed copies live next to them
# and are committed; raw source files + zips stay local for re-export only.
public/publications/Logos/
public/Logos-*.zip
